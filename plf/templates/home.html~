{% extends "base.html" %}

{% block header %}Home{% endblock %}

{% block content %}
	<p>Recent items:</p>
	<div id="container">
		{% for item in items %}
                <div class="masonryImage">
                                {% if item.status == True %}
                                <p>LOST</p>
                                {% else %}
                                <p>FOUND</p>
                                {% endif %}
                                {% if item.picture %}
                                <img src="..{{ item.picture.url }}" />
                                {% endif %}
				<p>Description: {{ item.desc }}</p>
				<p>ID: {{ item.id }}</p>
				<p>Category: {{ item.category   }}</p>
				<p>Location: {{ item.location }}</p>

				<form action="" method="post">{% csrf_token %}
					<input type="hidden" value="{{ item.id }}" name="identity">
			        <p>Your e-mail: <input type="text" name="email"></p>
                                {% if item.status == True %}
			        <input type="submit" value="I Found This!">
                                {% else %}
                                <input type="submit" value="This is mine!">
                                {% endif %}
			    </form>
               </div>
	{% endfor %}
        </div>

<script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
<script src="../media/jquery.masonry.min.js"></script>
<script>
/*   $( document ).ready(function() {
        alert( "#include <yolo.h>!" );
   });
*/
  $(function() {
     var $container = $('#container');
     $container.imagesLoaded( function() {
       $container.masonry({
         itemSelector : '.masonryImage',
         columnWidth : function( containerWidth ) {
             return containerWidth / 5;
         }
         isAnimated: true
       });
    });
  });
</script>	

{% endblock %}
